apiVersion: v1
kind: Secret
metadata:
  name: skillflow-secrets
  namespace: skillflow
type: Opaque
stringData:
  jwt-secret: "your-super-secret-jwt-key-change-in-production"
  db-password: "skillflow"
  oidc-client-secret: "your-oidc-client-secret"
  minio-access-key: "minioadmin"
  minio-secret-key: "minioadmin"
